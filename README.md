lib/MemCache.php contains class of the same name. It accepts ip, port and async flag on construct.

All public methods of the class are tested in tests folder in async and regular mode.


# drimsim-test
